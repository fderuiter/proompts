- **Vulnerability found**: Many high-complexity prompts rely on simple markdown bracketing (e.g., `[TASK]: {{task}}`) for variable interpolation instead of structural delimiters (e.g., `<task>{{task}}</task>`), rendering them highly vulnerable to prompt injection via unexpected markdown headers within user input. We must systematically enforce XML wrappers across all agentic and workflow prompts.
