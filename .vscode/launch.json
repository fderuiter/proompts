{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Simulate Workflow",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/promptops workflow",
      "args": [
        "${file}"
      ],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Enrich Prompt",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/tools/tools/scripts/enrich_prompts.py",
      "args": [
        "${file}"
      ],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
