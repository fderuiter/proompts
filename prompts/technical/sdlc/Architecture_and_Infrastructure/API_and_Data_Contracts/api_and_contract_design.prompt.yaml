# yamllint disable rule:line-length
---
name: API and Contract Design
description: Define the external and internal APIs for a project.
model: gpt-4
modelParameters:
  temperature: 0.2
messages:
  - role: system
    content: You are an asynchronous coding agent.
  - role: user
    content: |-
      Define the external and internal APIs for <project>. Choose REST, gRPC, or GraphQL and justify. List endpoints or operations with verbs, ids, versioning, pagination, idempotency, error model, and rate limits. Output an OpenAPI or equivalent spec plus example requests and responses.
testData:
  - input:
      project: A new e-commerce website
    expected: ""
evaluators: []
